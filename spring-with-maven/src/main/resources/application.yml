spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo-jpa
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

local:
  zone: Asia/Shanghai